# API vers Microservices en Python

## Requirements :

- Python 3.8 minimum (pour FastAPI)

## Installation :

1. `git clone` this repository
2. `cd api_microservices_py`
3. `pip install -r requirements.txt`